FROM python:3.9-slim
WORKDIR /app

COPY requirements_agent.txt .
RUN pip install --no-cache-dir -r requirements_agent.txt

COPY agent_service.py .

# create runtime dirs
RUN mkdir data models plots

EXPOSE 7000
CMD ["uvicorn", "agent_service:app", "--host", "0.0.0.0", "--port", "7000"]
